export function PendingCounter() {
  return <span aria-label="Đơn chờ đồng bộ" className="rounded-full border border-border bg-surface px-3 py-1 text-sm font-semibold text-text-primary">0 đơn chờ</span>
}
