import { zodResolver } from '@hookform/resolvers/zod'
import { useForm, type Resolver } from 'react-hook-form'
import { z } from 'zod'
import { Button } from '../../../../shared/components/ui/button'
import { Input } from '../../../../shared/components/ui/input'
import type { CategoryDto } from '../../../../features/admin/categories/api'
import type { OptionGroupDto } from '../../../../features/admin/option-groups/api'
const intMin = (label: string) => z.preprocess((v) => v === '' ? undefined : v, z.coerce.number({ error: `${label} phải là số` }).int(`${label} phải là số nguyên`).min(0, `${label} không được âm`))
const schema = z.object({ name: z.string().trim().min(1, 'Vui lòng nhập tên sản phẩm').max(200, 'Tên sản phẩm tối đa 200 ký tự'), categoryId: z.string().min(1, 'Vui lòng chọn danh mục'), priceVnd: intMin('Giá'), optionGroupIds: z.array(z.string()), isActive: z.boolean(), sortOrder: intMin('Thứ tự') })
export type ProductFormValues = z.infer<typeof schema>
type Props = { defaultValues: ProductFormValues; categories: CategoryDto[]; optionGroups: OptionGroupDto[]; submitLabel: string; isSubmitting?: boolean; errorMessage?: string | null; onSubmit: (values: ProductFormValues) => void | Promise<void> }
export function ProductForm({ defaultValues, categories, optionGroups, submitLabel, isSubmitting = false, errorMessage, onSubmit }: Props) { const knownIds = new Set(optionGroups.map((g) => g.id)); const { register, handleSubmit, watch, formState: { errors } } = useForm<ProductFormValues>({ resolver: zodResolver(schema.refine((v) => v.optionGroupIds.every((id) => knownIds.has(id)), { path: ['optionGroupIds'], message: 'Nhóm tùy chọn không hợp lệ' })) as Resolver<ProductFormValues>, defaultValues }); const isActive = watch('isActive'); return <form className="mt-4 space-y-5" onSubmit={handleSubmit((values) => onSubmit(values))} noValidate>{errorMessage ? <div role="alert" className="rounded-md border border-danger/40 p-3 text-sm text-danger">{errorMessage}</div> : null}<p className="rounded-md bg-surface-muted p-3 text-sm text-text-secondary">Thay đổi chỉ áp dụng cho đơn mới. Đơn cũ giữ nguyên giá tại thời điểm bán.</p><div className="space-y-2"><label className="text-sm font-medium" htmlFor="product-name">Tên sản phẩm</label><Input id="product-name" {...register('name')} />{errors.name ? <p className="text-sm text-danger">{errors.name.message}</p> : null}</div><div className="grid gap-4 md:grid-cols-2"><div className="space-y-2"><label className="text-sm font-medium" htmlFor="product-category">Danh mục</label><select id="product-category" className="min-h-touch w-full rounded-md border border-border bg-surface px-3 py-2" {...register('categoryId')}><option value="">Chọn danh mục</option>{categories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select>{errors.categoryId ? <p className="text-sm text-danger">{errors.categoryId.message}</p> : null}</div><div className="space-y-2"><label className="text-sm font-medium" htmlFor="product-price">Giá (VND)</label><Input id="product-price" type="number" min={0} {...register('priceVnd')} />{errors.priceVnd ? <p className="text-sm text-danger">{errors.priceVnd.message}</p> : null}</div></div><fieldset className="space-y-2 rounded-md border border-border p-3"><legend className="px-1 text-sm font-medium">Nhóm tùy chọn</legend>{optionGroups.length ? optionGroups.map((g) => <label key={g.id} className="flex min-h-touch items-center gap-3 text-sm"><input type="checkbox" className="h-4 w-4 accent-primary" value={g.id} {...register('optionGroupIds')} />{g.name} <span className="text-text-secondary">({g.options.length} tùy chọn)</span></label>) : <p className="text-sm text-text-secondary">Chưa có nhóm tùy chọn.</p>}{errors.optionGroupIds ? <p className="text-sm text-danger">{errors.optionGroupIds.message}</p> : null}</fieldset><div className="grid gap-4 md:grid-cols-2"><label className="flex min-h-touch items-center justify-between gap-4 rounded-md border border-border p-3 text-sm font-medium" htmlFor="product-active"><span>{isActive ? 'Đang bán' : 'Tạm tắt'}</span><input id="product-active" type="checkbox" className="h-5 w-5 accent-primary" {...register('isActive')} /></label><div className="space-y-2"><label className="text-sm font-medium" htmlFor="product-sort">Thứ tự</label><Input id="product-sort" type="number" min={0} {...register('sortOrder')} />{errors.sortOrder ? <p className="text-sm text-danger">{errors.sortOrder.message}</p> : null}</div></div><div className="flex justify-end gap-2 pt-2"><Button type="submit" disabled={isSubmitting}>{isSubmitting ? 'Đang lưu...' : submitLabel}</Button></div></form> }
